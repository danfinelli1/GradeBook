<div>
  Welcome, <%= @teacher.first_name %>!
</div>
<h1>My Classes!</h1>
<% @courses.each do |course| %>
<%=link_to("#{course.name}", course_show_path(course.id)) %>
<%= course.number %><br>
<% end %>
<br>
<%= link_to("New Course", new_course_path)%> <br>
<%= link_to("Edit Profile", edit_teacher_path)%><br>
<%= link_to("Delete Account", delete_user_path(current_user.id),:method => :delete, data: {confirm: "Are you sure you want to delete your account?"})%><br>
