<h1><%= @course.name%> </h1>
<br>
<% @assignments.each do |assignment| %>
  <h5><%=link_to("#{assignment.name}", assignment_show_path({course_id:@course.id, assignment_id:assignment.id}))%> <br></h5>
<% end %>
<%= link_to("New Assignment", new_assignment_path, class:"waves-effect waves-light btn")%> <br>
